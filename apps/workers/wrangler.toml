name = "kb-chatbot-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"
workers_dev = true
routes = [
  { pattern = "kb-api.runvision.ai", custom_domain = true }
]

[observability]
enabled = true

[triggers]
crons = ["*/3 * * * *", "0 * * * *"]  # 3분 DB keepalive, 매시간 마켓플레이스 동기화
